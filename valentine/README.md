# Сайт-приглашение на День Святого Валентина 💕

Простой и красивый сайт-приглашение, созданный на чистом HTML, CSS и JavaScript.

## Структура проекта

- `index.html` - основной HTML файл
- `style.css` - стили и анимации
- `script.js` - интерактивность и эффекты

## Как использовать

1. Просто откройте `index.html` в браузере
2. Можно настроить текст приглашения в `index.html`
3. Можно изменить цвета и стили в `style.css`

## Особенности

- Красивый градиентный фон
- Плавные анимации
- Интерактивные кнопки
- Адаптивный дизайн для мобильных устройств
- Эффект летающих сердечек при нажатии "Да"

## Настройка

Вы можете легко изменить:
- Текст приглашения в `index.html` (тег `<p class="message">`)
- Заголовок в `index.html` (тег `<h1 class="title">`)
- Цвета в `style.css` (градиенты в `.btn-yes` и `body`)
- Сообщения в `script.js` (массив `messages`)

